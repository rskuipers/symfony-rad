{% extends 'base.html.twig' %}

{% block title %}Chuckle{% endblock %}

{% block body %}
    <h1>Chuckle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chuckle.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ chuckle.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chuckle_index') }}">back to list</a>

    <a href="{{ path('app_chuckle_edit', {'id': chuckle.id}) }}">edit</a>

    {{ include('chuckle/_delete_form.html.twig') }}
{% endblock %}
