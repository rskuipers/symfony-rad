<form method="post" action="{{ path('app_chuckle_delete', {'id': chuckle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chuckle.id) }}">
    <button class="btn">Delete</button>
</form>
